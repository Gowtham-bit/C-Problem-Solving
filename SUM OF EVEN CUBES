#include <stdio.h>
int main() 
{
    int limit,num = 2,sum = 0;
    scanf("%d",&limit);
    while (num <= limit) 
	{
        int cube = num * num * num;
        sum += cube;
        num += 2;
    }
    printf("Sum of cubes of even numbers up to %d: %d\n", limit, sum);
    return 0;
}
